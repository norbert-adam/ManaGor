# MANAGOR



## TELEGRAM BOT 

### TODO

Listing/filtering Todos:
`listtodo <filter_name> <filter_value>`
    if no arguments --> list all
    if `<filter_name>` == "filters" --> return list of possible filters
    
